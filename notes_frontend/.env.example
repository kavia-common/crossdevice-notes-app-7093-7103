# PUBLIC_INTERFACE
# REACT_APP_API_BASE
# This configures the backend API base URL for the frontend to call.
# Example development value (update as appropriate for your environment):
# REACT_APP_API_BASE=http://localhost:3001
